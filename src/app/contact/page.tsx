import { Card } from "@/components/ui";
import { schoolContent } from "@/content/school";
import { PublicSiteNav } from "@/components/public-site-nav";
export const revalidate = 3600;
export default function ContactPage() { return <main className="min-h-screen px-5 py-6 sm:px-8 sm:py-10"><div className="mx-auto max-w-6xl"><PublicSiteNav /><div className="mt-12 grid gap-8 lg:grid-cols-[.8fr_1.2fr]"><div><p className="eyebrow">Contact</p><h1 className="font-display mt-4 text-5xl font-semibold leading-tight sm:text-6xl">Let’s talk about school.</h1></div><Card className="p-7 sm:p-10"><div className="grid gap-7 sm:grid-cols-2"><p><span className="eyebrow block">Admissions</span><a className="mt-2 block font-semibold text-[var(--accent-light)] underline-offset-4 hover:underline" href={`mailto:${schoolContent.contact.admissionsEmail}`}>{schoolContent.contact.admissionsEmail}</a></p><p><span className="eyebrow block">Office hours</span><span className="mt-2 block font-semibold">{schoolContent.contact.hours}</span></p><p className="sm:col-span-2"><span className="eyebrow block">Address</span><span className="mt-2 block font-semibold">{schoolContent.contact.address}</span></p></div></Card></div></div></main>; }
