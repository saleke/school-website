import Link from "next/link";
import { Card } from "@/components/ui";
import { PublicSiteNav } from "@/components/public-site-nav";
const stories = [{ title: "Welcome to a new school year", date: "September 2026", body: "A warm welcome to every student, teacher, and family joining our community." }, { title: "Learning beyond the classroom", date: "August 2026", body: "Clubs, reading, and thoughtful practice help every learner find their stride." }];
export const revalidate = 3600;
export default function NewsPage() { return <main className="min-h-screen px-5 pb-[calc(8rem+env(safe-area-inset-bottom))] pt-6 sm:px-8 sm:py-10"><div className="mx-auto max-w-6xl"><PublicSiteNav /><header className="mt-12 max-w-3xl"><p className="eyebrow">News</p><h1 className="font-display mt-4 text-5xl font-semibold sm:text-6xl">Notes from campus.</h1><p className="mt-5 text-lg leading-8 text-text-secondary">A few useful signals from the school community.</p></header><div className="mt-10 grid gap-5 sm:grid-cols-2">{stories.map(story => <Card key={story.title} className="p-7"><p className="eyebrow">{story.date}</p><h2 className="font-display mt-4 text-2xl font-semibold">{story.title}</h2><p className="mt-3 leading-7 text-text-secondary">{story.body}</p><Link href="/contact" className="mt-7 inline-flex text-sm font-semibold text-[var(--accent-light)]">Talk to the school →</Link></Card>)}</div></div></main>; }
