import Link from "next/link";
import { Card } from "@/components/ui";
const stories = [{ title: "Welcome to a new school year", date: "September 2026", body: "A warm welcome to every student, teacher, and family joining our community." }, { title: "Learning beyond the classroom", date: "August 2026", body: "Clubs, reading, and thoughtful practice help every learner find their stride." }];
export const revalidate = 3600;
export default function NewsPage() { return <main className="paper-grid min-h-screen px-5 py-8 sm:px-8 sm:py-12"><div className="mx-auto max-w-4xl"><Link href="/" className="text-sm font-semibold text-text-secondary">← School Platform</Link><header className="mt-8"><p className="text-xs font-bold uppercase tracking-[0.18em] text-text-secondary">News</p><h1 className="font-display mt-3 text-4xl font-semibold sm:text-6xl">Notes from campus.</h1></header><div className="mt-8 grid gap-5 sm:grid-cols-2">{stories.map(story => <Card key={story.title}><p className="text-xs font-bold uppercase tracking-[0.14em] text-text-secondary">{story.date}</p><h2 className="font-display mt-4 text-2xl font-semibold">{story.title}</h2><p className="mt-3 text-text-secondary">{story.body}</p></Card>)}</div></div></main>; }
